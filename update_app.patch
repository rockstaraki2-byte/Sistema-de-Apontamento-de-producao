--- src/App.tsx
+++ src/App.tsx
@@ -1292,4 +1292,6 @@
 }: {
   users: User[];
+  systemSettings?: SystemSettings[];
   onLogin: (u: User) => void;
 }) {
@@ -1363,6 +1365,6 @@
         <div className="flex flex-col items-center gap-1 mb-8">
-          <Crown size={48} className="text-[#00b14f] mb-2" />
-          <h1 className="text-3xl font-bold tracking-tight text-[#00b14f]">
-            IMPÉRIO
+          {systemSettings?.[0]?.companyLogoUrl && systemSettings[0].companyLogoUrl !== "/icon.png" ? (
+            <img src={systemSettings[0].companyLogoUrl} alt="Logo" className="h-16 object-contain mb-2 max-w-[200px]" />
+          ) : (
+            <Crown size={48} className="text-[#00b14f] mb-2" />
+          )}
+          <h1 className="text-3xl font-bold tracking-tight" style={{ color: systemSettings?.[0]?.primaryColor || '#00b14f' }}>
+            {systemSettings?.[0]?.companyName || "IMPÉRIO"}
           </h1>
-          <span className="text-[0.65rem] text-gray-500 font-medium tracking-[0.2em]">
-            ACESSÓRIOS PARA MÓVEIS
+          <span className="text-[0.65rem] text-gray-500 font-medium tracking-[0.2em] text-center">
+            {systemSettings?.[0]?.systemName || "ACESSÓRIOS PARA MÓVEIS"}
           </span>
@@ -12871,3 +12873,3 @@
   if (!currentUser) {
-    return <LoginScreen users={db.users} onLogin={setCurrentUser} />;
+    return <LoginScreen users={db.users} systemSettings={db.systemSettings} onLogin={setCurrentUser} />;
   }
@@ -12923,10 +12925,14 @@
         <header className="bg-black text-[#00b14f] p-4 flex justify-between items-center shadow-md shrink-0 border-b border-[#00b14f]/20">
           <div className="flex items-center gap-2">
-            <Crown size={28} className="text-[#00b14f]" />
+            {db.systemSettings?.[0]?.companyLogoUrl && db.systemSettings[0].companyLogoUrl !== "/icon.png" ? (
+              <img src={db.systemSettings[0].companyLogoUrl} alt="Logo" className="h-8 object-contain max-w-[120px]" />
+            ) : (
+              <Crown size={28} className="text-[#00b14f]" style={{ color: db.systemSettings?.[0]?.primaryColor || '#00b14f' }} />
+            )}
             <div className="flex flex-col leading-none">
-              <h1 className="text-xl font-bold tracking-tight">IMPÉRIO</h1>
+              <h1 className="text-xl font-bold tracking-tight" style={{ color: db.systemSettings?.[0]?.primaryColor || '#00b14f' }}>{db.systemSettings?.[0]?.companyName || "IMPÉRIO"}</h1>
               <span className="text-[0.6rem] text-gray-400 font-medium tracking-widest">
-                ACESSÓRIOS PARA MÓVEIS
+                {db.systemSettings?.[0]?.systemName || "ACESSÓRIOS PARA MÓVEIS"}
               </span>
             </div>
